skill_id: cdc_v1

issue_types:
  - cdc_failure

specialization: |
  Focus: CDC failure, capture lag, cleanup lag, and TempDB/version store pressure.

user_prompt_template: |
  Phan tich CDC failure finding sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools:
  - get_cdc_status

optional_tools:
  - get_tempdb_usage
  - get_wait_stats
  - get_query_stats

model: claude-haiku-4-5-20251001
max_tool_rounds: 3
max_tokens: 1500
max_cost_usd: 0.05
include_fields: []
