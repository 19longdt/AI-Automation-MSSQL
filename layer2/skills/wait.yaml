skill_id: wait_v1

issue_types:
  - wait_anomaly

specialization: |
  Focus: wait type anomaly, workload correlation, and practical remediation.

user_prompt_template: |
  Phan tich wait anomaly finding sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools:
  - get_wait_stats

optional_tools:
  - get_plan_analysis
  - get_query_structure
  - get_table_context
  - get_query_stats
  - get_blocking_chain
  - get_resource_governor_stats
  - get_memory_pressure
  - get_analysis_history

model: claude-haiku-4-5-20251001
max_tool_rounds: 3
max_tokens: 1500
max_cost_usd: 0.05
include_fields: []
