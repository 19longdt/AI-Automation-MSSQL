skill_id: tempdb_v1

issue_types:
  - tempdb_pressure

specialization: |
  Focus: TempDB pressure, breakdown by version store, spills, and user objects.

user_prompt_template: |
  Phan tich TempDB pressure finding sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools:
  - get_tempdb_usage

optional_tools:
  - get_cdc_status
  - get_query_stats
  - get_query_structure
  - get_plan_analysis
  - get_table_context
  - get_wait_stats
  - get_memory_grant
  - get_analysis_history

model: claude-haiku-4-5-20251001
max_tool_rounds: 3
max_tokens: 1500
max_cost_usd: 0.05
include_fields: []
