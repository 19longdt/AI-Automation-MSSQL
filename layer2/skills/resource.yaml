skill_id: resource_v1

issue_types:
  - resource_pool_spike

specialization: |
  Focus: Resource Governor pool spike, culprit workload, and impact on queued requests.

user_prompt_template: |
  Phan tich resource pool spike finding sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools:
  - get_resource_governor_stats

optional_tools:
  - get_query_stats
  - get_wait_stats
  - get_blocking_chain

model: claude-haiku-4-5-20251001
max_tool_rounds: 3
max_tokens: 1500
max_cost_usd: 0.05
include_fields: []
