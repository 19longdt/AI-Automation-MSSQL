skill_id: maintenance_v1

issue_types:
  - job_failure
  - backup_gap
  - dbcc_overdue

specialization: |
  Focus: maintenance failures, backup gap, va DBCC overdue.

user_prompt_template: |
  Phan tich maintenance issue sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools: []

optional_tools:
  - get_recent_findings
  - get_query_stats
  - get_wait_stats
  - get_analysis_history

model: claude-haiku-4-5-20251001
max_tool_rounds: 2
max_tokens: 1500
max_cost_usd: 0.05
include_fields: []
