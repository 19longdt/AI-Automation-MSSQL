skill_id: blocking_v1

issue_types:
  - blocking_chain
  - blocked_query_snapshot
  - blocked_query_trend

specialization: |
  Focus: blocking chain, head blocker, long transaction, va recurrence.

user_prompt_template: |
  Phan tich blocking finding sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools:
  - get_blocking_chain
  - get_wait_stats

optional_tools:
  - get_query_stats
  - get_query_store_history
  - get_recent_findings
  - get_analysis_history

model: claude-sonnet-4-6
max_tool_rounds: 4
max_tokens: 2000
max_cost_usd: 0.10
include_fields: []
