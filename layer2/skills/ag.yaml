skill_id: ag_v1

issue_types:
  - ag_lag

specialization: |
  Focus: Always On AG replication lag. Xac dinh nguyen nhan send queue, redo queue, va rui ro failover.

user_prompt_template: |
  Phan tich AG lag finding sau:

  Issue: {issue_type} | Severity: {severity}
  Node: {node} ({role}) | Detected: {detected_at}
  Metrics: {metrics_json}

required_tools:
  - get_ag_status

optional_tools:
  - get_wait_stats
  - get_query_stats
  - get_resource_governor_stats

model: claude-haiku-4-5-20251001
max_tool_rounds: 3
max_tokens: 1500
max_cost_usd: 0.05
include_fields: []
